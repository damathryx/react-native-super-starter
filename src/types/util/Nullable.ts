export type Nullable<T> = T | null;

export type NullString = {
  String: string;
  Valid: boolean;
};

export type NullFloat = {
  Float64: number;
  Valid: boolean;
};

export type NullInt = {
  Int64: number;
  Valid: boolean;
};
